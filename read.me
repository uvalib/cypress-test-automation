Installation and use
--------------------

1) Install cypress (assumes node/npm installed)
npm install cypress

2) Example run
node_modules/.bin/cypress run --headless --spec "cypress/e2e/*.cy.js"

Other notes
-----------

1) Due to AWS bot rules we have configured, automated testing will only run on your workstation when on the VPN.
